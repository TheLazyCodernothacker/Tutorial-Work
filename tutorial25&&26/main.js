{
    var name = prompt();
    if(name !== "Caleb"){
        console.log('Get outta here!')};
    var age = prompt();
    if(age >= 18){
        console.log('Welcome oldy!')
    }
}
{
    var age = prompt('Age?');
    var name = prompt('Name?');
    if(age < 13 && !(name === "Caleb")){
        console.log('Welcome!')}
    }
