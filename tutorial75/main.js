//object orienting program needs inheritance
//this is when objects take stuff from other objects like plaigarism
//This can be done with prototypes that save space
//This can also avoid duplication of a complicated method
//If we have two objects both with the same method, they can both use the global prototype.
//A prototype can have a prototype, cuz why not?
//Eventually the chain will end, and then there will be a null.
//Object.prototype is a default