{
    let name = prompt("What is my name?");
    //unary operators, binary operators,q
    let points = name === "Caleb" ? 10 : 0;
    console.log(`You have ${points} points`);
}

//the basic way
if(name === "Caleb"){
    let points = 10;}else{
    let points = 0;
    }